# Semantic Gate Decision

**CLOSED — explanation-only; no provider call was made.**

- Structured semantic inputs: 0
- The frozen pools do not contain `part_role`, `interface_type`, `assembly_family`, or `functional_relation`.
- Anonymous geometry summaries are not submitted for semantic judgment.
- DeepSeek cannot change scores, ranking, acceptance, rejection, or review routing.
- Re-open only after the calibration rules in `semantic_calibration_report.json` pass on a functional holdout.

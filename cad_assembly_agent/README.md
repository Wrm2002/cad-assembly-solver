# CAD Assembly Agent — Stage 1

本目录只完成真实 STEP benchmark、B-Rep graph/shape-map 和人工真值模板。
当前不训练模型、不调用 Qwen、不修改主 solver、不生成 CAD。

数据源保持只读：`../sw/1` 至 `../sw/5`。生成的大型 graph 放在
`D:\Model_match_agent_data\step_real_benchmark`。

